default:
  callback: "on_cancel"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./default/template/error.yaml"


cancel_ticket_metro_soft:
  condition:
    operation:
      type: AND
      input:
        value:
          - operation:
              type: EQUAL
              title: Check search message code
              input:
                value:
                  - operation:
                      type: READ
                      input:
                        value: "req_body.message.order_id"
                  - ORDER_ID_1_METRO
          - operation:
              type: EQUAL
              title: Check search message code
              input:
                value:
                  - operation:
                      type: READ
                      input:
                        value: "req_body.message.descriptor.code"
                  - SOFT_CANCEL
          - operation:
              type: EQUAL
              title: Check search message code
              input:
                value:
                  - operation:
                      type: READ
                      input:
                        value: "req_body.context.domain"
                  - "ONDC:TRV11"
  callback: "on_cancel"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./metro/template/on_cancel_soft.yaml"


cancel_ticket_metro_confirm:
  condition:
    operation:
      type: AND
      input:
        value:
          - operation:
              type: EQUAL
              title: Check search message code
              input:
                value:
                  - operation:
                      type: READ
                      input:
                        value: "req_body.message.order_id"
                  - ORDER_ID_1_METRO
          - operation:
              type: EQUAL
              title: Check search message code
              input:
                value:
                  - operation:
                      type: READ
                      input:
                        value: "req_body.message.descriptor.code"
                  - CONFIRM_CANCEL
          - operation:
              type: EQUAL
              title: Check search message code
              input:
                value:
                  - operation:
                      type: READ
                      input:
                        value: "req_body.context.domain"
                  - "ONDC:TRV11"
  callback: "on_cancel"
  uri:
    $ref: ""
  delay: 300
  payload:
    $ref: "./metro/template/on_cancel_confirm.yaml"
